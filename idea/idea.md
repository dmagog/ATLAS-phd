# ATLAS: концепция проекта

Статус файла:
- концептуальный документ ранней стадии;
- не является основным источником истины после `Milestone 1`;
- зафиксированные решения уточняются и детализируются в `docs/product-proposal.md` и `docs/technical_specification.md`.

## 1. Название и назначение

**ATLAS (Assistant for Technical Learning & Attestation Support)** — интеллектуальный ассистент для подготовки к сдаче кандидатского минимума по технической специальности.

Каналы взаимодействия:
- веб-чат (приоритетный канал MVP, в том числе для отображения формул через LaTeX);
- Telegram-бот (дополнительный канал MVP для коротких сценариев и доступа "на ходу").

Кандидатский минимум — совокупность обязательных экзаменов в системе подготовки научных и научно-педагогических кадров (аспирантура).

## 2. Проблема

Подготовка к кандидатскому минимуму требует работы с большим объемом разрозненных материалов:
- учебники;
- научные статьи;
- методические пособия;
- конспекты и дополнительные материалы.

Ключевые сложности:
- ручная систематизация информации;
- сложность быстрого поиска релевантных фрагментов;
- отсутствие адаптированной под конкретного обучающегося проверки знаний.

## 3. Целевая аудитория

- аспиранты;
- соискатели ученых степеней;
- слушатели программ подготовки к кандидатским экзаменам.

## 4. Цель проекта

Создать интеллектуального ассистента, который помогает готовиться к кандидатскому минимуму за счет:
- ответов на вопросы по учебным темам;
- самопроверки знаний;
- опоры на ограниченную и управляемую базу учебных материалов.

Пилотная область:
- один конкретный профильный технический экзамен;
- при этом внутри базы могут быть материалы из разных тематических курсов, относящихся к выбранному экзамену.

## 5. Общая концепция решения

ATLAS реализуется как агентная RAG-система (Retrieval-Augmented Generation + explicit orchestration):
1. Пользователь задает вопрос в web-чате или Telegram-боте.
2. `Planner` определяет маршрут (`Q&A`, `Self-check`, `clarify`).
3. В ветке `Q&A` система выполняет retrieval релевантных фрагментов, формирует ответ и проверяет его через `Verifier` (hard-gate).
4. В ветке `Self-check` формируется набор вопросов, выполняется оценка и формируется обратная связь.
5. Пользователь получает ответ с источниками или контролируемый отказ с пояснением.

Дополнительно система использует LLM для:
- классификации намерения;
- генерации ответов и заданий;
- оценивания открытых ответов и выдачи развивающей обратной связи.

## 6. Базовые функциональные возможности (черновой перечень)

### 6.1. Диалоговый интерфейс
- web-интерфейс как основной формат взаимодействия в MVP;
- Telegram как дополнительный канал для упрощенных сценариев;
- поддержка вопросов по темам подготовки;
- переключаемый режим ответа (кратко/подробно/учебный формат);
- поддержка русского и английского языков;
- сессионная память Q&A и сохранение результатов самопроверки между сессиями в MVP;
- продвинутая долгосрочная аналитика обучения вынесена в зону расширения.

### 6.2. Роли и доступ
- роль `администратор`: загрузка и обновление базы знаний;
- роль `пользователь`: обучение, ответы на вопросы, самопроверка.

### 6.3. Работа с базой знаний
- загрузка материалов только администратором;
- ручной импорт пакетами;
- допустимые форматы MVP: `pdf`, `docx`, `txt`, `md`;
- формат `djvu` не входит в MVP (может поддерживаться позже через отдельный конвертер/OCR);
- предварительная обработка и индексация;
- векторизация и поиск релевантных фрагментов через retrieval tool.

### 6.4. Ответы с опорой на источники
- маршрутизация запроса через `Planner`;
- формирование структурированного ответа;
- подробное указание использованных источников (файл, раздел/глава, фрагмент);
- проверка ответа через `Verifier`;
- честный и контролируемый отказ от ответа при недостатке релевантных данных;
- рекомендация переформулировать запрос при отказе.

### 6.5. Самопроверка
- генерация набора вопросов по теме (тестовые + открытые);
- прием ответов пользователя;
- автоматическая проверка, оценка и развивающая обратная связь;
- по целевой архитектуре: `Question Generator -> Evaluator -> Feedback Agent`;
- в PoC: упрощенная реализация `Question Generator -> Evaluator` с базовым feedback;
- ограничение по времени в MVP не вводится.

## 7. Нефункциональные требования (черновой перечень)

- язык реализации: Python;
- рекомендуемый стек MVP: `aiogram` + `FastAPI` + `PostgreSQL`;
- модульная архитектура;
- возможность локального запуска;
- документированный код;
- базовые практики безопасности (хранение ключей в переменных окружения, разграничение ролей, аудит действий администратора).

## 8. Базовые пользовательские сценарии

### Сценарий 1: Получение ответа
1. Пользователь задает вопрос по теме.
2. `Planner` определяет, что запрос относится к ветке `Q&A`.
3. Система ищет релевантные материалы через retrieval tool.
4. `Verifier` проверяет достаточность evidence.
5. Система возвращает структурированный ответ с источниками или отказ с пояснением.

### Сценарий 2: Самопроверка
1. Пользователь указывает тему.
2. `Planner` направляет запрос в ветку `Self-check`.
3. Система генерирует тест/вопросы.
4. Пользователь отвечает.
5. Система оценивает ответы и дает обратную связь.

### Сценарий 3: Отсутствие данных
1. Пользователь задает вопрос.
2. Retrieval и `Verifier` определяют, что релевантные материалы отсутствуют или недостаточны.
3. Система явно сообщает об ограничении, не генерирует ответ "по памяти" и предлагает следующий шаг.

## 9. Границы первой версии (MVP, для уточнения)

В первую версию целесообразно включить:
- web-чат как основной интерфейс MVP;
- Telegram-бот как дополнительный канал;
- 2 режима: "вопрос-ответ" и "самопроверка";
- работу с ограниченным набором документов;
- агентный RAG-конвейер с явной оркестрацией;
- ссылки на источники в каждом ответе;
- метрики качества RAG и проверки знаний;
- локальный запуск для демонстрации и тестирования.

Отложить на следующие этапы:
- сложная персонализация траектории обучения;
- режим преподавателя;
- расширенная аналитика прогресса и персонализированные дашборды обучения;
- поддержка `djvu` без предварительной конвертации.

## 10. Маппинг открытых вопросов на решения

| Открытый вопрос | Принятое решение | Где зафиксировано |
|---|---|---|
| Базовая архитектура: plain RAG или agentic RAG | Выбран `agentic RAG` с явным графом оркестрации | `docs/product-proposal.md`, `docs/technical_specification.md` |
| Основной канал MVP | Выбран `web-first`, Telegram остается дополнительным каналом | `README.md`, `docs/product-proposal.md`, `docs/technical_specification.md` |
| Политика надежности | Выбран `hard-gate` verifier с честным отказом | `docs/product-proposal.md`, `docs/technical_specification.md`, `docs/governance.md` |
| Retrieval стратегия PoC | В PoC используется `vector-only`, hybrid вынесен в roadmap | `docs/product-proposal.md` |
| Модель памяти | `Q&A` в рамках сессии, история `self-check` между сессиями | `docs/product-proposal.md`, `docs/technical_specification.md` |
| Self-check pipeline | Целевая схема: `Question Generator -> Evaluator -> Feedback Agent`; в PoC — упрощенная ветка | `docs/product-proposal.md` |
| Проверка качества | Зафиксирован пилотный `eval-set v0` и процесс разметки | `docs/product-proposal.md` |

Файл сохраняет концептуальный контекст проекта, а все решения после `Milestone 1` должны проверяться по основным документам в `docs/`.
